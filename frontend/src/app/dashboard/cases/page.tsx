import { FileCheck2, Filter, Plus, Search } from "lucide-react";

export default function CasesPage() {
  return <div><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-teal-700">Operations</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.035em]">Authorizations</h1><p className="mt-2 text-sm text-slate-500">Manage every case from intake through payer resolution.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-[#123448] px-4 py-2.5 text-sm font-semibold text-white"><Plus className="size-4" /> New authorization</button></div>
    <div className="mt-8 rounded-xl border border-slate-200 bg-white"><div className="flex flex-col gap-3 border-b border-slate-200 p-4 sm:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm text-slate-400"><Search className="size-4" /><input className="w-full py-2.5 outline-none" placeholder="Search case ID, patient, or service" /></label><button className="inline-flex items-center justify-center gap-2 rounded-lg border border-slate-200 px-4 text-sm text-slate-600"><Filter className="size-4" /> Filters</button></div><div className="grid min-h-80 place-items-center p-8 text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-xl bg-slate-100 text-slate-500"><FileCheck2 className="size-5" /></span><h2 className="mt-4 font-semibold">No authorizations yet</h2><p className="mt-2 max-w-sm text-sm leading-6 text-slate-500">Create your first case or connect Salesforce to begin importing authorization context.</p><button className="mt-5 rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold">Create sample case</button></div></div></div>
  </div>;
}
