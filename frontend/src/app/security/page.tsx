import { CorporatePage } from "@/components/corporate-page";

export default function SecurityPage() {
  return <CorporatePage eyebrow="Security" title="Trust boundaries designed into the product." intro="Security is an architectural constraint at Authora—not a badge. The foundation below describes implemented controls and explicit product boundaries.">{[["Tenant isolation","Protected business records belong to an organization and server-established tenant context controls access."],["Credential protection","Salesforce access and refresh tokens are encrypted at rest and never enter browser storage."],["Auditable operations","Material connection, provisioning, and workflow events produce organization-scoped audit records."],["Data minimization","PHI is excluded from logs and uncontrolled telemetry, and integrations request only necessary scope."],["Human review","Automated recommendations remain advisory, source-linked, and reviewable before external submission."],["Responsible AI","No autonomous medical-necessity decision is delegated to an AI model."]].map(([title,text]) => <article id={title==="Responsible AI"?"responsible-ai":undefined} key={title} className="rounded-xl border border-white/10 bg-white/[.025] p-7"><h2 className="text-lg font-semibold">{title}</h2><p className="mt-3 text-sm leading-7 text-slate-400">{text}</p></article>)}</CorporatePage>;
}
