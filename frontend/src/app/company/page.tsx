import { CorporatePage } from "@/components/corporate-page";

export default function CompanyPage() {
  return <CorporatePage eyebrow="Company" title="Infrastructure for accountable healthcare operations." intro="Authora Health is building the operational layer between clinical evidence, payer requirements, and the teams responsible for moving care forward.">{[["Our mission","Reduce avoidable administrative delay without compromising clinical judgment, security, or accountability."],["Our market","We begin with US specialty-care organizations managing complex, high-value authorization workflows."],["Our product principle","Automate coordination and evidence work while keeping consequential decisions reviewable by people."],["Our operating model","Authora connects to existing systems instead of pretending to replace the EHR, Salesforce, or payer channels."]].map(([title,text]) => <article id={title==="Our product principle"?"principles":undefined} key={title} className="rounded-xl border border-white/10 bg-white/[.025] p-7"><h2 className="text-lg font-semibold">{title}</h2><p className="mt-3 text-sm leading-7 text-slate-400">{text}</p></article>)}</CorporatePage>;
}
